---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install python for Ansible
      raw: test -e /usr/bin/python3  || (yum -y update && yum install -y python3) | python3 -m pip install flake8
      become: true
      changed_when: false
